# Present in this repo after building; regenerated by Gradle / Kotlin
.gradle/
.kotlin/
build/

# Machine-local Android SDK path (never commit)
local.properties

# CI-generated signing files if reproduced locally (do not commit)
keystore.properties
filepipe-release.keystore
